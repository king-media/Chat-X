const Error = () => {
  const root = document.createElement('main')

  root.innerHTML = `
    <main>
      <h1>Sorry something went wrong...</h1>
    </main>
  `

  return root
}
  

export default Error
